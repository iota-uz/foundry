/**
 * Planning Layout
 *
 * Minimal layout for the full-screen planning view.
 */

export default function PlanLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
